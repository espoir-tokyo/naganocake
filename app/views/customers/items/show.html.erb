
	<div class="col-xs-3">
	<table class="table table-bordered">
		<thead>
			<tr>
				<th>ジャンル検索</th>
			</tr>
		</thead>
		<tbody>
				<% @genres.each do |genre| %>
    			<tr><td><%= link_to genre.name, "/customers/genres/#{genre.id}" %></td></tr>
				<% end %>
		</tbody>
	</table>
</div>

<div class="col-xs-8">

<div class="col-xs-4">
<%= attachment_image_tag(@item, :image, :fill, 200, 200) %>
</div>

<div class="col-xs-8">
    <%= @item.name %><br>
    <%= @item.description %><br>
    ￥<%= @item.price_with_tax %>（税込）<br>
    <div class="dropdown">
	<button class="btn btn-default dropdown-toggle" type="button" data-toggle="dropdown">
		個数選択
		<span class="caret"></span>
	</button>
	<ul class="dropdown-menu" role="menu">
		<li role="presentation"><a href="#">１</a></li>
		<li role="presentation"><a href="#">２</a></li>
		<li role="presentation"><a href="#">３</a></li>
	</ul>
</div>
    <button type="button" class="btn btn-primary">カートに入れる</button>
</div>

</div>
